<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>.
  <%= link_to "Settings", edit_user_registration_path %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete %>
<% end %>
<h1>RareFishMap#index</h1>
<p>Find me in app/views/rare_fish_map/index.html.erb</p>

<div class="container">
  <div class="starter-template">
    <div class="row text-left">
      <div class="col-sm-6">
        ここに詳細が記述される<p id="notice"><%= notice %></p>

        <h1>Rare Fishes</h1>
        
        <table>
          <thead>
            <tr>
              <th>Fish</th>
              <th>User</th>
              <th>Title</th>
              <th>Content</th>
              <th>Image</th>
              <th>Adless</th>
              <th>Like</th>
              <th colspan="3"></th>
            </tr>
          </thead>
        
          <tbody>
            <% @rare_fishes.each do |rare_fish| %>
              <tr>
                <td><%= rare_fish.fish_id %></td>
                <td><%= rare_fish.user %></td>
                <td><%= rare_fish.title %></td>
                <td><%= rare_fish.content %></td>
                <td><%= rare_fish.image %></td>
                <td><%= rare_fish.adless %></td>
                <td><%= rare_fish.like %></td>
                <td><%= link_to 'Show', rare_fish %></td>
                <td><%= link_to 'Edit', edit_rare_fish_path(rare_fish) %></td>
                <td><%= link_to 'Destroy', rare_fish, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        
        <br>
        
        <%= link_to 'New Rare Fish', new_rare_fish_path %>
        
      </div>
      <div class="col-sm-6">
        ここにコメントが記述される
        <p id="notice"><%= notice %></p>

<h1>Fish Tweets</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Image</th>
      <th>Content</th>
      <th>Like</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fish_tweets.each do |fish_tweet| %>
      <tr>
        <td><%= fish_tweet.user.name %></td>
        <td><%= fish_tweet.image %></td>
        <td><%= fish_tweet.content %></td>
        <td><%= fish_tweet.like %></td>
        <td><%= link_to 'Show', fish_tweet %></td>
        <% if user_signed_in? && fish_tweet.user_id == current_user.id %>

        
        <td><%= link_to 'Edit', edit_fish_tweet_path(fish_tweet) %></td>
        <td><%= button_to 'Destroy', fish_tweet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fish Tweet', new_fish_tweet_path %>
        
      </div>
    </div>
  </div>
</div>

<!--コンテナを分ける--><br>

<div class="container">
  <div class="starter-template">
    <div class="row text-left">
      <div class="col-sm-6">
        ここに詳細が記述される<p id="notice"><%= notice %></p>

        <h1>Rare Fishes</h1>
        
        <table>
          <thead>
            <tr>
              <th>Fish</th>
              <th>User</th>
              <th>Title</th>
              <th>Content</th>
              <th>Image</th>
              <th>Adless</th>
              <th>Like</th>
              <th colspan="3"></th>
            </tr>
          </thead>
        
          <tbody>
            <% @rare_fishes.each do |rare_fish| %>
              <tr>
                <td><%= rare_fish.fish_id[1] %></td>
                
                <td><%= rare_fish.user %></td>
                <td><%= rare_fish.title %></td>
                <td><%= rare_fish.content %></td>
                <td><%= rare_fish.image %></td>
                <td><%= rare_fish.adless %></td>
                <td><%= rare_fish.like %></td>
                <td><%= link_to 'Show', rare_fish %></td>
              </tr>
            <% end %>

          
          </tbody>
        </table>
        
        <br>
        
        <%= link_to 'New Rare Fish', new_rare_fish_path %>
        
      </div>
      <div class="col-sm-6">
        ここにコメントが記述される<br>
        <% player = ["test","test2","test3"] %>
        <% player2 = ["test","Nice Here!!","test3"] %>
        <% player3 = ["test","test2","test3"] %>
        <% team = [player,player2,player3] %>
        <% price = 2500 * 1.05 %>
        <% array = [[10,20],[30,40]] %>
        <% array.each do |a| %>
        <%= p a %>
        <p>ーーーー</p>
        
        <p>ーーーー</p>
        <% end %>
<p>
スタートポイント：<%= team %>：エンドポイント。<br>
スタートポイント：<%= @rare_fishes %>：エンドポイント。
</p>
      </div>
    </div>
  </div>
</div>

<!--さらにコンテナを分ける--><br>
<%= x = 0 %>
<% @rare_fishes.each do |rare_fish| %>
<tr>
  <div class="container">
    <div class="starter-template">
      <div class="row text-left">
        <div class="col-sm-6">
  <td><%= rare_fish.fish_id %></td>
  
  <td><%= rare_fish.user %></td>
  <td><%= rare_fish.title %></td>
  <td><%= rare_fish.content %></td>
  <td><%= rare_fish.image %></td>
  <td><%= rare_fish.adless %></td>
  <td><%= rare_fish.like %></td>
  <td><%= link_to 'Show', rare_fish %></td>
</tr></div>
<div class="col-sm-6">
  <%= x = x+1 %>ここにコメントが記載される</div>
</div></div></div><br>
<% end %>


<br><br><%= link_to 'Show list',rare_fishes_path %>

